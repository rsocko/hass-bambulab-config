# Legacy Model Catalog Tests

This folder contains archived tests that covered transitional Manyfold-backed behavior.

These tests are retained for historical reference only and are intentionally outside the active `tests/` tree so they do not run in the default pytest suite.

Current archived tests:
- `test_manyfold_upload_adapter.py`
