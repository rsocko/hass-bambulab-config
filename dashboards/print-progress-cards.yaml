# Bambu Lab Print Progress Cards
#
# Four cards showing print progress with animated icons:
#   Row 1 (horizontal): Layer Progress | Print Progress  ← KPI-style (large value)
#   Row 2 (horizontal): Time Remaining | Est. Completion ← compact horizontal buttons
#
# Animations are active only when the printer is actively printing or preparing.
# When paused, idle, finished, or failed the icons are static.
#
# Icon animations:
#   Layer Progress  - layers bounce upward, simulating layers piling up
#   Print Progress  - icon spins, indicating active progress
#   Time Remaining  - clock rotates, like spinning clock hands
#   Est. Completion - flag waves gently; turns solid green when the print finishes
#
# KPI card design (Layer Progress & Print Progress):
#   - Value displayed large and centred (takes majority of card space)
#   - Label in small text at the bottom-left
#   - Status icon at the bottom-right
#   - Uses custom:button-card (requires button-card from HACS)
#
# Estimated Completion smart time format:
#   Same day      → "4:32 PM"
#   Next day      → "4:32 PM tomorrow"
#   Within a week → "4:32 PM on Wednesday"
#   Farther away  → "4:32 PM on 4/12/26"
#
# INSTALLATION:
#   1. Replace every occurrence of YOUR_PRINTER_NAME with your actual printer
#      entity prefix (e.g. ntk_ryansoffice_3dprinter).
#      Use Find & Replace — each card block contains multiple instances.
#   2. Paste the vertical-stack block below into your Home Assistant dashboard
#      (Dashboard → Edit → ⋮ → Raw configuration editor).
#   3. Ensure the following custom cards are installed from HACS:
#        - button-card (required for Layer Progress & Print Progress KPI cards)
#        - mushroom    (required for Time Remaining & Est. Completion)
#        - card-mod    (required for animations on mushroom cards)
#
# REQUIRED ENTITIES:
#   sensor.YOUR_PRINTER_NAME_print_status          (e.g. printing / pause / idle)
#   sensor.YOUR_PRINTER_NAME_print_progress        (0–100 % value)
#   sensor.YOUR_PRINTER_NAME_end_time              (datetime of estimated completion)
#   sensor.print_layer_progress_formatted          (custom template — see note below)
#   sensor.print_time_remaining_formatted          (custom template — see note below)
#
# NOTE: sensor.print_layer_progress_formatted and sensor.print_time_remaining_formatted
# are custom Home Assistant template sensors that format the raw layer count and
# remaining-time values from the Bambu Lab integration into human-readable strings.
# If you do not already have these, you can substitute:
#   sensor.YOUR_PRINTER_NAME_current_layer_num     for layer progress
#   sensor.YOUR_PRINTER_NAME_remaining_time        for time remaining
# and adjust the secondary template accordingly.

# =============================================================================
# FULL 2×2 GRID (RECOMMENDED — all four cards in a single vertical-stack)
# =============================================================================
type: vertical-stack
cards:
  # ---------------------------------------------------------------------------
  # ROW 1: Layer Progress + Print Progress (KPI-style — large value, icon bottom-right)
  # ---------------------------------------------------------------------------
  - type: horizontal-stack
    cards:

      # -----------------------------------------------------------------------
      # LAYER PROGRESS CARD
      # KPI display: large value centred, label small at bottom-left, icon bottom-right.
      # Icon bounces upward while printing, simulating layers piling up.
      # -----------------------------------------------------------------------
      - type: custom:button-card
        entity: sensor.print_layer_progress_formatted
        show_name: false
        show_state: false
        show_icon: false
        tap_action:
          action: more-info
        custom_fields:
          kpi: |
            [[[
              const state = entity?.state;
              const value = (state && state !== 'unavailable' && state !== 'unknown') ? state : 'N/A';
              const status = states['sensor.YOUR_PRINTER_NAME_print_status']?.state?.toLowerCase() || '';
              const stage = states['sensor.YOUR_PRINTER_NAME_current_stage']?.state?.toLowerCase() || '';
              const isActive = status === 'printing' || status === 'prepare' || stage === 'printing';
              const isPaused = status === 'pause' || stage === 'pause';
              const iconColor = isActive ? '#2196F3' : isPaused ? '#FF9800' : '#9E9E9E';
              const animClass = isActive ? ' kpi-layer-anim' : '';
              return `<div class="kpi-wrap">
                <div class="kpi-value">${value}</div>
                <div class="kpi-footer">
                  <span class="kpi-label">Layer Progress</span>
                  <ha-icon icon="mdi:layers-triple" class="kpi-icon${animClass}" style="color:${iconColor};"></ha-icon>
                </div>
              </div>`;
            ]]]
        styles:
          card:
            - padding: 12px
            - min-height: 100px
          custom_fields:
            kpi:
              - width: 100%
        extra_styles: |
          .kpi-wrap { display:flex; flex-direction:column; min-height:78px; }
          .kpi-value { font-size:40px; font-weight:700; text-align:center; line-height:1; padding:8px 0; flex-grow:1; display:flex; align-items:center; justify-content:center; }
          .kpi-footer { display:flex; justify-content:space-between; align-items:center; }
          .kpi-label { font-size:11px; font-weight:500; opacity:0.7; }
          .kpi-icon { --mdc-icon-size:18px; }
          @keyframes layer-stack { 0%,60%,100% { transform:translateY(0); } 30% { transform:translateY(-5px); } }
          .kpi-layer-anim { animation:layer-stack 1.8s ease-in-out infinite; }

      # -----------------------------------------------------------------------
      # PRINT PROGRESS CARD
      # KPI display: large value centred, label small at bottom-left, icon bottom-right.
      # Icon spins continuously while printing.
      # -----------------------------------------------------------------------
      - type: custom:button-card
        entity: sensor.YOUR_PRINTER_NAME_print_progress
        show_name: false
        show_state: false
        show_icon: false
        tap_action:
          action: more-info
        custom_fields:
          kpi: |
            [[[
              const state = entity?.state;
              const value = (state && state !== 'unavailable' && state !== 'unknown') ? `${state}%` : 'N/A';
              const status = states['sensor.YOUR_PRINTER_NAME_print_status']?.state?.toLowerCase() || '';
              const stage = states['sensor.YOUR_PRINTER_NAME_current_stage']?.state?.toLowerCase() || '';
              const isActive = status === 'printing' || status === 'prepare' || stage === 'printing';
              const isPaused = status === 'pause' || stage === 'pause';
              const iconColor = isActive ? '#4CAF50' : isPaused ? '#FF9800' : '#9E9E9E';
              const animClass = isActive ? ' kpi-progress-anim' : '';
              return `<div class="kpi-wrap">
                <div class="kpi-value">${value}</div>
                <div class="kpi-footer">
                  <span class="kpi-label">Print Progress</span>
                  <ha-icon icon="mdi:rotate-right" class="kpi-icon${animClass}" style="color:${iconColor};"></ha-icon>
                </div>
              </div>`;
            ]]]
        styles:
          card:
            - padding: 12px
            - min-height: 100px
          custom_fields:
            kpi:
              - width: 100%
        extra_styles: |
          .kpi-wrap { display:flex; flex-direction:column; min-height:78px; }
          .kpi-value { font-size:40px; font-weight:700; text-align:center; line-height:1; padding:8px 0; flex-grow:1; display:flex; align-items:center; justify-content:center; }
          .kpi-footer { display:flex; justify-content:space-between; align-items:center; }
          .kpi-label { font-size:11px; font-weight:500; opacity:0.7; }
          .kpi-icon { --mdc-icon-size:18px; }
          @keyframes progress-spin { from { transform:rotate(0deg); } to { transform:rotate(360deg); } }
          .kpi-progress-anim { animation:progress-spin 3s linear infinite; }

  # ---------------------------------------------------------------------------
  # ROW 2: Time Remaining + Estimated Completion (compact horizontal button style)
  # ---------------------------------------------------------------------------
  - type: horizontal-stack
    cards:

      # -----------------------------------------------------------------------
      # TIME REMAINING CARD
      # Compact horizontal button layout. Clock icon rotates while printing.
      # -----------------------------------------------------------------------
      - type: custom:mushroom-template-card
        entity: sensor.print_time_remaining_formatted
        primary: Time Remaining
        secondary: "{{ states('sensor.print_time_remaining_formatted') }}"
        icon: mdi:clock-time-four
        icon_color: >-
          {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
          {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
          {% if status in ['printing', 'prepare'] or stage == 'printing' %}orange
          {% elif status == 'pause' or stage == 'pause' %}amber
          {% else %}grey{% endif %}
        layout: horizontal
        tap_action:
          action: more-info
        card_mod:
          style: >
            {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
            {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
            {% if status in ['printing', 'prepare'] or stage == 'printing' %}
            @keyframes clock-spin {
              from { transform: rotate(0deg); }
              to { transform: rotate(360deg); }
            }
            mushroom-shape-icon { animation: clock-spin 4s linear infinite; }
            {% endif %}
            .primary { font-size: 12px !important; font-weight: 600; }
            .secondary { font-size: 14px !important; font-weight: 700; }

      # -----------------------------------------------------------------------
      # ESTIMATED COMPLETION CARD
      # Compact horizontal button layout.
      # Displays a smart human-readable completion time:
      #   Same day      → "4:32 PM"
      #   Next day      → "4:32 PM tomorrow"
      #   Within a week → "4:32 PM on Wednesday"
      #   Farther away  → "4:32 PM on 4/12/26"
      # Flag waves gently while printing; turns green when finished.
      # -----------------------------------------------------------------------
      - type: custom:mushroom-template-card
        entity: sensor.YOUR_PRINTER_NAME_end_time
        primary: Est. Completion
        secondary: >-
          {% set state = states('sensor.YOUR_PRINTER_NAME_end_time') %}
          {% if state not in ['unavailable', 'unknown'] and state is not none and state != '' %}
            {% set end_ts = as_timestamp(state) | int(0) %}
            {% set now_ts = as_timestamp(now()) | int(0) %}
            {% if end_ts > 0 %}
              {% set time_str = end_ts | timestamp_custom('%-I:%M %p', true) %}
              {% set end_date = end_ts | timestamp_custom('%Y-%m-%d', true) %}
              {% set today    = now_ts | timestamp_custom('%Y-%m-%d', true) %}
              {% set tomorrow = (now_ts + 86400) | timestamp_custom('%Y-%m-%d', true) %}
              {% set days_diff = ((end_ts - now_ts) / 86400) | int(0) %}
              {% if end_date == today %}{{ time_str }}
              {% elif end_date == tomorrow %}{{ time_str }} tomorrow
              {% elif days_diff < 7 %}{{ time_str }} on {{ end_ts | timestamp_custom('%A', true) }}
              {% else %}{{ time_str }} on {{ end_ts | timestamp_custom('%-m/%-d/%y', true) }}
              {% endif %}
            {% else %}N/A
            {% endif %}
          {% else %}N/A
          {% endif %}
        icon: mdi:flag-checkered
        icon_color: >-
          {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
          {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
          {% if status == 'finish' %}green
          {% elif status in ['printing', 'prepare'] or stage == 'printing' %}purple
          {% elif status == 'pause' or stage == 'pause' %}orange
          {% else %}grey{% endif %}
        layout: horizontal
        tap_action:
          action: more-info
        card_mod:
          style: >
            {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
            {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
            {% if status in ['printing', 'prepare'] or stage == 'printing' %}
            @keyframes flag-wave {
              0%, 100% { transform: scale(1) rotate(0deg); }
              25% { transform: scale(1.08) rotate(-4deg); }
              75% { transform: scale(1.08) rotate(4deg); }
            }
            mushroom-shape-icon { animation: flag-wave 2s ease-in-out infinite; }
            {% endif %}
            .primary { font-size: 12px !important; font-weight: 600; }
            .secondary { font-size: 14px !important; font-weight: 700; }

---

# =============================================================================
# INDIVIDUAL CARDS (if you prefer to place each card separately)
# =============================================================================

# LAYER PROGRESS — individual card (KPI style)
type: custom:button-card
entity: sensor.print_layer_progress_formatted
show_name: false
show_state: false
show_icon: false
tap_action:
  action: more-info
custom_fields:
  kpi: |
    [[[
      const state = entity?.state;
      const value = (state && state !== 'unavailable' && state !== 'unknown') ? state : 'N/A';
      const status = states['sensor.YOUR_PRINTER_NAME_print_status']?.state?.toLowerCase() || '';
      const stage = states['sensor.YOUR_PRINTER_NAME_current_stage']?.state?.toLowerCase() || '';
      const isActive = status === 'printing' || status === 'prepare' || stage === 'printing';
      const isPaused = status === 'pause' || stage === 'pause';
      const iconColor = isActive ? '#2196F3' : isPaused ? '#FF9800' : '#9E9E9E';
      const animClass = isActive ? ' kpi-layer-anim' : '';
      return `<div class="kpi-wrap">
        <div class="kpi-value">${value}</div>
        <div class="kpi-footer">
          <span class="kpi-label">Layer Progress</span>
          <ha-icon icon="mdi:layers-triple" class="kpi-icon${animClass}" style="color:${iconColor};"></ha-icon>
        </div>
      </div>`;
    ]]]
styles:
  card:
    - padding: 12px
    - min-height: 100px
  custom_fields:
    kpi:
      - width: 100%
extra_styles: |
  .kpi-wrap { display:flex; flex-direction:column; min-height:78px; }
  .kpi-value { font-size:40px; font-weight:700; text-align:center; line-height:1; padding:8px 0; flex-grow:1; display:flex; align-items:center; justify-content:center; }
  .kpi-footer { display:flex; justify-content:space-between; align-items:center; }
  .kpi-label { font-size:11px; font-weight:500; opacity:0.7; }
  .kpi-icon { --mdc-icon-size:18px; }
  @keyframes layer-stack { 0%,60%,100% { transform:translateY(0); } 30% { transform:translateY(-5px); } }
  .kpi-layer-anim { animation:layer-stack 1.8s ease-in-out infinite; }

---

# PRINT PROGRESS — individual card (KPI style)
type: custom:button-card
entity: sensor.YOUR_PRINTER_NAME_print_progress
show_name: false
show_state: false
show_icon: false
tap_action:
  action: more-info
custom_fields:
  kpi: |
    [[[
      const state = entity?.state;
      const value = (state && state !== 'unavailable' && state !== 'unknown') ? `${state}%` : 'N/A';
      const status = states['sensor.YOUR_PRINTER_NAME_print_status']?.state?.toLowerCase() || '';
      const stage = states['sensor.YOUR_PRINTER_NAME_current_stage']?.state?.toLowerCase() || '';
      const isActive = status === 'printing' || status === 'prepare' || stage === 'printing';
      const isPaused = status === 'pause' || stage === 'pause';
      const iconColor = isActive ? '#4CAF50' : isPaused ? '#FF9800' : '#9E9E9E';
      const animClass = isActive ? ' kpi-progress-anim' : '';
      return `<div class="kpi-wrap">
        <div class="kpi-value">${value}</div>
        <div class="kpi-footer">
          <span class="kpi-label">Print Progress</span>
          <ha-icon icon="mdi:rotate-right" class="kpi-icon${animClass}" style="color:${iconColor};"></ha-icon>
        </div>
      </div>`;
    ]]]
styles:
  card:
    - padding: 12px
    - min-height: 100px
  custom_fields:
    kpi:
      - width: 100%
extra_styles: |
  .kpi-wrap { display:flex; flex-direction:column; min-height:78px; }
  .kpi-value { font-size:40px; font-weight:700; text-align:center; line-height:1; padding:8px 0; flex-grow:1; display:flex; align-items:center; justify-content:center; }
  .kpi-footer { display:flex; justify-content:space-between; align-items:center; }
  .kpi-label { font-size:11px; font-weight:500; opacity:0.7; }
  .kpi-icon { --mdc-icon-size:18px; }
  @keyframes progress-spin { from { transform:rotate(0deg); } to { transform:rotate(360deg); } }
  .kpi-progress-anim { animation:progress-spin 3s linear infinite; }

---

# TIME REMAINING — individual card (compact horizontal button)
type: custom:mushroom-template-card
entity: sensor.print_time_remaining_formatted
primary: Time Remaining
secondary: "{{ states('sensor.print_time_remaining_formatted') }}"
icon: mdi:clock-time-four
icon_color: >-
  {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
  {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
  {% if status in ['printing', 'prepare'] or stage == 'printing' %}orange
  {% elif status == 'pause' or stage == 'pause' %}amber
  {% else %}grey{% endif %}
layout: horizontal
tap_action:
  action: more-info
card_mod:
  style: >
    {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
    {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
    {% if status in ['printing', 'prepare'] or stage == 'printing' %}
    @keyframes clock-spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    mushroom-shape-icon { animation: clock-spin 4s linear infinite; }
    {% endif %}
    .primary { font-size: 12px !important; font-weight: 600; }
    .secondary { font-size: 14px !important; font-weight: 700; }

---

# ESTIMATED COMPLETION — individual card (compact horizontal button)
type: custom:mushroom-template-card
entity: sensor.YOUR_PRINTER_NAME_end_time
primary: Est. Completion
secondary: >-
  {% set state = states('sensor.YOUR_PRINTER_NAME_end_time') %}
  {% if state not in ['unavailable', 'unknown'] and state is not none and state != '' %}
    {% set end_ts = as_timestamp(state) | int(0) %}
    {% set now_ts = as_timestamp(now()) | int(0) %}
    {% if end_ts > 0 %}
      {% set time_str = end_ts | timestamp_custom('%-I:%M %p', true) %}
      {% set end_date = end_ts | timestamp_custom('%Y-%m-%d', true) %}
      {% set today    = now_ts | timestamp_custom('%Y-%m-%d', true) %}
      {% set tomorrow = (now_ts + 86400) | timestamp_custom('%Y-%m-%d', true) %}
      {% set days_diff = ((end_ts - now_ts) / 86400) | int(0) %}
      {% if end_date == today %}{{ time_str }}
      {% elif end_date == tomorrow %}{{ time_str }} tomorrow
      {% elif days_diff < 7 %}{{ time_str }} on {{ end_ts | timestamp_custom('%A', true) }}
      {% else %}{{ time_str }} on {{ end_ts | timestamp_custom('%-m/%-d/%y', true) }}
      {% endif %}
    {% else %}N/A
    {% endif %}
  {% else %}N/A
  {% endif %}
icon: mdi:flag-checkered
icon_color: >-
  {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
  {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
  {% if status == 'finish' %}green
  {% elif status in ['printing', 'prepare'] or stage == 'printing' %}purple
  {% elif status == 'pause' or stage == 'pause' %}orange
  {% else %}grey{% endif %}
layout: horizontal
tap_action:
  action: more-info
card_mod:
  style: >
    {% set status = states('sensor.YOUR_PRINTER_NAME_print_status') | lower %}
    {% set stage = states('sensor.YOUR_PRINTER_NAME_current_stage') | lower %}
    {% if status in ['printing', 'prepare'] or stage == 'printing' %}
    @keyframes flag-wave {
      0%, 100% { transform: scale(1) rotate(0deg); }
      25% { transform: scale(1.08) rotate(-4deg); }
      75% { transform: scale(1.08) rotate(4deg); }
    }
    mushroom-shape-icon { animation: flag-wave 2s ease-in-out infinite; }
    {% endif %}
    .primary { font-size: 12px !important; font-weight: 600; }
    .secondary { font-size: 14px !important; font-weight: 700; }
