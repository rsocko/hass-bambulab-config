# Print History Card — full-width responsive archive grid.
# Archive cards render from direct Bambuddy websocket queries so filtering and
# paging stay server-side without materializing large page payloads into HA
# entity state.

type: custom:print-history-browser-card
title: Print History
hide_title: true
show_empty_state: true