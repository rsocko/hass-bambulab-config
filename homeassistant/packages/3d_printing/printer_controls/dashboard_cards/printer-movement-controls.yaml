# ============================================================
# Printer Movement Controls Card
# ============================================================
# Replicates the "Show Controls" page from ha-bambulab-cards (a1-screen-card)
# independently of the full Print Status card.
#
# Uses two bambu_lab services (from the ha-bambulab integration):
#   - bambu_lab.move_axis  { device_id, axis: X|Y|Z|HOME, distance: number }
#   - bambu_lab.extrude_retract  { device_id, type: retract|extrude }
#
# Layout (circular XY joypad):
#   [ XY Joypad ] | [ Z-axis (Bed) ] | [ Extruder ]
#
# Color scheme:
#   ±1 mm buttons  — medium gray  rgba(158,158,158,0.3)
#   ±10 mm buttons — blue tint    rgba(66,165,245,0.25)
#   HOME button    — blue accent   rgba(33,150,243,0.15)
#
# ⚠ WARNING: Moving axes while a print is in progress can damage your model
# or printer.  These controls are only safe when the printer is idle.
# ============================================================

type: vertical-stack
cards:
  # ── Active-print warning ───────────────────────────────────
  - type: custom:button-card
    entity: sensor.ntk_ryansoffice_3dprinter_current_stage
    show_name: false
    show_state: false
    show_icon: false
    tap_action:
      action: none
    custom_fields:
      content: |
        [[[
          const stage = states['sensor.ntk_ryansoffice_3dprinter_current_stage']?.state ?? '';
          const isPrinting = ['printing', 'running', 'prepare', 'init'].includes(stage.toLowerCase());
          return isPrinting
            ? `<div class="warn-wrap"><div class="warn">⚠️ Printer is active - use movement controls with caution</div></div>`
            : '';
        ]]]
    styles:
      card:
        - padding: 4px 12px
        - background: transparent
        - box-shadow: none
        - border: none
      custom_fields:
        content:
          - width: 100%
          - display: flex
          - justify-content: center
    extra_styles: |
      .warn {
        display: block;
        font-size: 14px;
        color: #FF9800;
        text-align: center;
      }
      .warn-wrap {
        width: 100%;
        display: flex;
        justify-content: center;
      }

  # ── Main controls: [ XY ] | [ Z ] | [ Extruder ] ──────────
  - type: horizontal-stack
    card_mod:
      style: |
        #root {
          display: grid !important;
          grid-template-columns: minmax(0, 4fr) minmax(68px, 1fr) minmax(68px, 1fr);
          align-items: start;
          column-gap: 12px;
          row-gap: 12px;
          padding: 0 6px;
        }
        #root > * {
          min-width: 0 !important;
          max-width: none !important;
          width: auto !important;
        }
        @media (max-width: 760px) {
          #root {
            grid-template-columns: 1fr 1fr;
          }
          #root > *:first-child {
            grid-column: 1 / -1;
          }
        }
    cards:
      # ── XY Joypad (circular layout) ───────────────────────
      - type: vertical-stack
        cards:
          - type: custom:button-card
            name: X / Y Axis
            show_name: true
            show_state: false
            show_icon: false
            tap_action:
              action: none
            styles:
              card:
                - padding: 2px
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 32px
              name:
                - font-size: 15px
                - font-weight: 600
                - opacity: '0.7'
                - white-space: nowrap
                - justify-self: center
          - type: picture-elements
            aspect_ratio: 1/1
            image: /local/3d_printing/printer_controls/printer_xy_control.svg
            card_mod:
              style: |
                ha-card {
                  background: transparent;
                  box-shadow: none;
                }
            elements:
              # Outer ring sectors: +/- 10 mm (full clickable area)
              - type: custom:button-card
                icon: mdi:chevron-double-up
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "Y"
                    distance: 10
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(66, 165, 245, 0.22)
                    - border: 1px solid rgba(66, 165, 245, 0.32)
                    - clip-path: polygon(15.4% 15.4%, 50% 3%, 84.6% 15.4%, 74.8% 25.2%, 50% 16%, 25.2% 25.2%)
                    - display: flex
                    - justify-content: center
                    - align-items: flex-start
                    - padding-top: 12px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 24px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(66, 165, 245, 0.42) !important; border-color: rgba(66, 165, 245, 0.68) !important; }
                  ha-card:active { transform: scale(0.98); }
              - type: custom:button-card
                icon: mdi:chevron-double-down
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "Y"
                    distance: -10
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(66, 165, 245, 0.22)
                    - border: 1px solid rgba(66, 165, 245, 0.32)
                    - clip-path: polygon(15.4% 84.6%, 50% 97%, 84.6% 84.6%, 74.8% 74.8%, 50% 84%, 25.2% 74.8%)
                    - display: flex
                    - justify-content: center
                    - align-items: flex-end
                    - padding-bottom: 12px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 24px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(66, 165, 245, 0.42) !important; border-color: rgba(66, 165, 245, 0.68) !important; }
                  ha-card:active { transform: scale(0.98); }
              - type: custom:button-card
                icon: mdi:chevron-double-left
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "X"
                    distance: -10
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(66, 165, 245, 0.22)
                    - border: 1px solid rgba(66, 165, 245, 0.32)
                    - clip-path: polygon(15.4% 15.4%, 3% 50%, 15.4% 84.6%, 25.2% 74.8%, 16% 50%, 25.2% 25.2%)
                    - display: flex
                    - justify-content: flex-start
                    - align-items: center
                    - padding-left: 12px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 24px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(66, 165, 245, 0.42) !important; border-color: rgba(66, 165, 245, 0.68) !important; }
                  ha-card:active { transform: scale(0.98); }
              - type: custom:button-card
                icon: mdi:chevron-double-right
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "X"
                    distance: 10
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(66, 165, 245, 0.22)
                    - border: 1px solid rgba(66, 165, 245, 0.32)
                    - clip-path: polygon(84.6% 15.4%, 97% 50%, 84.6% 84.6%, 74.8% 74.8%, 84% 50%, 74.8% 25.2%)
                    - display: flex
                    - justify-content: flex-end
                    - align-items: center
                    - padding-right: 12px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 24px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(66, 165, 245, 0.42) !important; border-color: rgba(66, 165, 245, 0.68) !important; }
                  ha-card:active { transform: scale(0.98); }

              # Inner ring sectors: +/- 1 mm (full clickable area)
              - type: custom:button-card
                icon: mdi:chevron-up
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "Y"
                    distance: 1
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(158, 158, 158, 0.28)
                    - border: 1px solid rgba(158, 158, 158, 0.35)
                    - clip-path: polygon(25.2% 25.2%, 50% 16%, 74.8% 25.2%, 61.8% 38.2%, 50% 34.5%, 38.2% 38.2%)
                    - display: flex
                    - justify-content: center
                    - align-items: flex-start
                    - padding-top: 18px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 22px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(158, 158, 158, 0.5) !important; border-color: rgba(196, 196, 196, 0.75) !important; }
                  ha-card:active { transform: scale(0.98); }
              - type: custom:button-card
                icon: mdi:chevron-down
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "Y"
                    distance: -1
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(158, 158, 158, 0.28)
                    - border: 1px solid rgba(158, 158, 158, 0.35)
                    - clip-path: polygon(25.2% 74.8%, 50% 84%, 74.8% 74.8%, 61.8% 61.8%, 50% 65.5%, 38.2% 61.8%)
                    - display: flex
                    - justify-content: center
                    - align-items: flex-end
                    - padding-bottom: 18px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 22px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(158, 158, 158, 0.5) !important; border-color: rgba(196, 196, 196, 0.75) !important; }
                  ha-card:active { transform: scale(0.98); }
              - type: custom:button-card
                icon: mdi:chevron-left
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "X"
                    distance: -1
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(158, 158, 158, 0.28)
                    - border: 1px solid rgba(158, 158, 158, 0.35)
                    - clip-path: polygon(25.2% 25.2%, 16% 50%, 25.2% 74.8%, 38.2% 61.8%, 34.5% 50%, 38.2% 38.2%)
                    - display: flex
                    - justify-content: flex-start
                    - align-items: center
                    - padding-left: 18px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 22px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(158, 158, 158, 0.5) !important; border-color: rgba(196, 196, 196, 0.75) !important; }
                  ha-card:active { transform: scale(0.98); }
              - type: custom:button-card
                icon: mdi:chevron-right
                show_name: false
                show_state: false
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "X"
                    distance: 1
                style:
                  left: 50%
                  top: 50%
                  width: 100%
                  height: 100%
                  transform: translate(-50%, -50%)
                styles:
                  card:
                    - border-radius: 0
                    - background: rgba(158, 158, 158, 0.28)
                    - border: 1px solid rgba(158, 158, 158, 0.35)
                    - clip-path: polygon(74.8% 25.2%, 84% 50%, 74.8% 74.8%, 61.8% 61.8%, 65.5% 50%, 61.8% 38.2%)
                    - display: flex
                    - justify-content: flex-end
                    - align-items: center
                    - padding-right: 18px
                  icon:
                    - color: var(--primary-text-color)
                    - width: 22px
                extra_styles: |
                  ha-card { transition: transform 0.12s, background-color 0.12s, border-color 0.12s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.03); background-color: rgba(158, 158, 158, 0.5) !important; border-color: rgba(196, 196, 196, 0.75) !important; }
                  ha-card:active { transform: scale(0.98); }

              # Home in center
              - type: custom:button-card
                icon: mdi:home
                show_name: false
                show_state: false
                aspect_ratio: 1/1
                tap_action:
                  action: call-service
                  service: bambu_lab.move_axis
                  service_data:
                    device_id: "210dfdfa64085e8cf073e50eae757d90"
                    axis: "HOME"
                    distance: 0
                  confirmation:
                    text: >-
                      Home the printer? This brings the heat bed to the nozzle —
                      remove any model from the bed first to avoid damage.
                style:
                  left: 50%
                  top: 50%
                  width: 30%
                styles:
                  card:
                    - border-radius: 50%
                    - background: rgba(33, 150, 243, 0.15)
                    - border: 1px solid rgba(33, 150, 243, 0.45)
                  icon:
                    - color: '#2196F3'
                    - width: 26px
                extra_styles: |
                  ha-card { transition: transform 0.15s, background-color 0.15s, border-color 0.15s; cursor: pointer; }
                  ha-card:hover { transform: scale(1.08); background-color: rgba(33, 150, 243, 0.32) !important; border-color: rgba(33, 150, 243, 0.78) !important; }
                  ha-card:active { transform: scale(0.93); }

      # ── Z-axis (Bed) Controls ──────────────────────────────
      - type: vertical-stack
        cards:
          - type: custom:button-card
            name: Bed (Z)
            show_name: true
            show_state: false
            show_icon: false
            tap_action:
              action: none
            styles:
              card:
                - padding: 2px
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 32px
              name:
                - font-size: 15px
                - font-weight: 600
                - opacity: '0.7'
                - white-space: nowrap
                - justify-self: center
          # Z +10 (blue tint)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:chevron-double-up
            tap_action:
              action: call-service
              service: bambu_lab.move_axis
              service_data:
                device_id: "210dfdfa64085e8cf073e50eae757d90"
                axis: "Z"
                distance: -10
            styles:
              card:
                - padding: 4px
                - border-radius: 12px
                - height: 48px
                - background-color: rgba(66, 165, 245, 0.25)
              icon:
                - color: var(--primary-text-color)
                - width: 20px
            extra_styles: |
              ha-card { transition: transform 0.15s, background-color 0.15s; cursor: pointer; }
              ha-card:hover { transform: scale(1.08); background-color: rgba(66, 165, 245, 0.45) !important; }
              ha-card:active { transform: scale(0.93); }
          # Z +1 (gray)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:chevron-up
            tap_action:
              action: call-service
              service: bambu_lab.move_axis
              service_data:
                device_id: "210dfdfa64085e8cf073e50eae757d90"
                axis: "Z"
                distance: -1
            styles:
              card:
                - padding: 4px
                - border-radius: 12px
                - height: 48px
                - background-color: rgba(158, 158, 158, 0.3)
              icon:
                - color: var(--primary-text-color)
                - width: 20px
            extra_styles: |
              ha-card { transition: transform 0.15s, background-color 0.15s; cursor: pointer; }
              ha-card:hover { transform: scale(1.08); background-color: rgba(158, 158, 158, 0.5) !important; }
              ha-card:active { transform: scale(0.93); }
          # Bed icon (centered)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:train-car-flatbed
            tap_action:
              action: none
            styles:
              card:
                - padding: 0
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 48px
              icon:
                - color: var(--primary-text-color)
                - opacity: '0.6'
                - width: 22px
          # Z -1 (gray)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:chevron-down
            tap_action:
              action: call-service
              service: bambu_lab.move_axis
              service_data:
                device_id: "210dfdfa64085e8cf073e50eae757d90"
                axis: "Z"
                distance: 1
            styles:
              card:
                - padding: 4px
                - border-radius: 12px
                - height: 48px
                - background-color: rgba(158, 158, 158, 0.3)
              icon:
                - color: var(--primary-text-color)
                - width: 20px
            extra_styles: |
              ha-card { transition: transform 0.15s, background-color 0.15s; cursor: pointer; }
              ha-card:hover { transform: scale(1.08); background-color: rgba(158, 158, 158, 0.5) !important; }
              ha-card:active { transform: scale(0.93); }
          # Z -10 (blue tint)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:chevron-double-down
            tap_action:
              action: call-service
              service: bambu_lab.move_axis
              service_data:
                device_id: "210dfdfa64085e8cf073e50eae757d90"
                axis: "Z"
                distance: 10
            styles:
              card:
                - padding: 4px
                - border-radius: 12px
                - height: 48px
                - background-color: rgba(66, 165, 245, 0.25)
              icon:
                - color: var(--primary-text-color)
                - width: 20px
            extra_styles: |
              ha-card { transition: transform 0.15s, background-color 0.15s; cursor: pointer; }
              ha-card:hover { transform: scale(1.08); background-color: rgba(66, 165, 245, 0.45) !important; }
              ha-card:active { transform: scale(0.93); }

      # ── Extruder Controls ──────────────────────────────────
      - type: vertical-stack
        cards:
          - type: custom:button-card
            name: Extruder
            show_name: true
            show_state: false
            show_icon: false
            tap_action:
              action: none
            styles:
              card:
                - padding: 2px
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 32px
              name:
                - font-size: 15px
                - font-weight: 600
                - opacity: '0.7'
                - white-space: nowrap
                - justify-self: center
          # Spacer (aligns retract with Z +1)
          - type: custom:button-card
            show_name: false
            show_state: false
            show_icon: false
            tap_action:
              action: none
            styles:
              card:
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 48px
                - padding: 0
          # Retract (gray — matches Z ±1)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:chevron-up
            tap_action:
              action: call-service
              service: bambu_lab.extrude_retract
              service_data:
                device_id: "210dfdfa64085e8cf073e50eae757d90"
                type: "retract"
            styles:
              card:
                - padding: 4px
                - border-radius: 12px
                - height: 48px
                - background-color: rgba(158, 158, 158, 0.3)
              icon:
                - color: var(--primary-text-color)
                - width: 20px
            extra_styles: |
              ha-card { transition: transform 0.15s, background-color 0.15s; cursor: pointer; }
              ha-card:hover { transform: scale(1.08); background-color: rgba(158, 158, 158, 0.5) !important; }
              ha-card:active { transform: scale(0.93); }
          # Nozzle icon (centered)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:printer-3d-nozzle-outline
            tap_action:
              action: none
            styles:
              card:
                - padding: 0
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 48px
              icon:
                - color: var(--primary-text-color)
                - opacity: '0.6'
                - width: 22px
          # Extrude (gray — matches Z ±1)
          - type: custom:button-card
            show_name: false
            show_state: false
            icon: mdi:chevron-down
            tap_action:
              action: call-service
              service: bambu_lab.extrude_retract
              service_data:
                device_id: "210dfdfa64085e8cf073e50eae757d90"
                type: "extrude"
            styles:
              card:
                - padding: 4px
                - border-radius: 12px
                - height: 48px
                - background-color: rgba(158, 158, 158, 0.3)
              icon:
                - color: var(--primary-text-color)
                - width: 20px
            extra_styles: |
              ha-card { transition: transform 0.15s, background-color 0.15s; cursor: pointer; }
              ha-card:hover { transform: scale(1.08); background-color: rgba(158, 158, 158, 0.5) !important; }
              ha-card:active { transform: scale(0.93); }
          # Spacer (aligns with Z -10)
          - type: custom:button-card
            show_name: false
            show_state: false
            show_icon: false
            tap_action:
              action: none
            styles:
              card:
                - background: transparent
                - box-shadow: none
                - border: none
                - height: 48px
                - padding: 0

  # ── Legend ──────────────────────────────────────────────────
  - type: custom:button-card
    show_name: false
    show_state: false
    show_icon: false
    tap_action:
      action: none
    custom_fields:
      legend: |
        [[[return `
          <div class="legend">
            <span><span class="swatch blue"></span> = ±10 mm&ensp;·&ensp;<span class="swatch gray"></span> = ±1 mm</span>
            <span><ha-icon icon="mdi:home" style="--mdc-icon-size:14px;"></ha-icon> = Home axes (requires confirmation)</span>
          </div>
        `]]]
    styles:
      card:
        - padding: 4px 12px 8px
        - background: transparent
        - box-shadow: none
        - border: none
      custom_fields:
        legend:
          - width: 100%
    extra_styles: |
      .legend { display:flex; flex-direction:column; gap:2px; font-size:10px; opacity:0.5; text-align:center; }
      .swatch { display:inline-block; width:10px; height:10px; border-radius:3px; vertical-align:middle; }
      .swatch.blue { background:rgba(66,165,245,0.5); }
      .swatch.gray { background:rgba(158,158,158,0.5); }
