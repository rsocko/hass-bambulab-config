# Core — foundational template sensors and automations.
# Docs: docs/features/core/README.md

template:
  - sensor: !include_dir_merge_list template_sensors

recorder:
  exclude:
    entities:
      - sensor.spoolman_filament_totals

automation: !include_dir_merge_list automations
