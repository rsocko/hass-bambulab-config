# WLED Package Placeholder

> **STATUS: SUPERSEDED** — The HA package now exists at `homeassistant/packages/3d_printing/wled/` with automations, scripts, and helpers deployed. See [ha-state-machine-package.md](ha-state-machine-package.md) for details.

~~The feature folder `homeassistant/packages/3d_printing/wled/` is reserved for Home Assistant package assets related to WLED automation and sensors.~~

The package is now populated with:

- `automations/` — State machine orchestrator
- `scripts/` — Transition and preset application scripts
- `helpers/` — input_boolean, input_select, input_text entities
